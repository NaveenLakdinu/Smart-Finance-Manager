# Fix: Business Dashboard Subscription Card + Lock Button

## Issue 1: Subscription card navigates to wrong Activity

**File:** `app/src/main/java/com/example/smartfinancialmanagement/BusinessDashboardActivity.java` (line 99)

The click handler navigates to `SubscriptionActivity` (a notification preferences / onboarding screen), but it should navigate to `SubscriptionManagerActivity` (the subscription management hub) — which is what every other dashboard does.

**Fix:** Change line 99:
```java
// FROM:
findViewById(R.id.cardManageSubscription).setOnClickListener(v -> startActivity(new Intent(this, SubscriptionActivity.class)));
// TO:
findViewById(R.id.cardManageSubscription).setOnClickListener(v -> startActivity(new Intent(this, SubscriptionManagerActivity.class)));
```

---

## Issue 2: Lock button not working in Business & Student dashboards

The `btnSecurity` FrameLayout with the lock icon exists in both dashboard layouts but no click listener is attached.

### 2a. BusinessDashboardActivity.java

**File:** `app/src/main/java/com/example/smartfinancialmanagement/BusinessDashboardActivity.java`

1. Add `setupSecurityButton()` method (copy from `WorkerDashboardActivity.java` lines 129-161)
2. Call `setupSecurityButton()` from `initializeViews()` (after existing view setup)

### 2b. StudentDashboardActivity.java

**File:** `app/src/main/java/com/example/smartfinancialmanagement/StudentDashboardActivity.java`

1. Add `setupSecurityButton()` method (copy from `WorkerDashboardActivity.java` lines 129-161)
2. Call `setupSecurityButton()` from `initViews()` (after existing view setup)

---

## Files to modify
1. `app/src/main/java/com/example/smartfinancialmanagement/BusinessDashboardActivity.java`
2. `app/src/main/java/com/example/smartfinancialmanagement/StudentDashboardActivity.java`

## Reference file (no changes needed)
- `app/src/main/java/com/example/smartfinancialmanagement/WorkerDashboardActivity.java` — contains the working `setupSecurityButton()` implementation to copy from

## Verification
1. `./gradlew assembleDebug` — confirm build succeeds
2. On device/emulator: tap subscription card on business dashboard → should open `SubscriptionManagerActivity`
3. On device/emulator: tap lock button on business dashboard → should show PIN Lock Security dialog
4. On device/emulator: tap lock button on student dashboard → should show PIN Lock Security dialog

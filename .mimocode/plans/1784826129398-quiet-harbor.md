# Plan: Add Notification Panel to All Dashboards

## Goal
Add the notification bell button and in-app notification panel dialog to the Student Dashboard and Student-Worker Hybrid Dashboard (the two dashboards currently missing it).

## Current State
- **Have notification panel**: WorkerDashboard, Dashboard, BusinessDashboard, MultiAccountDashboard
- **Missing notification panel**: StudentDashboard, StudentWorkerHybridDashboard

## Changes

### 1. Student Dashboard Layout
**File**: `app/src/main/res/layout/activity_student_dashboard.xml`

The layout already has a notification icon (ImageView at line 139) but it lacks an `id` and click handler. Change the existing notification `MaterialCardView` (lines 129-145) to:
- Add `android:id="@+id/btnNotifications"` to the existing `MaterialCardView`
- Keep the existing `ic_notification` icon inside

### 2. Student Dashboard Java
**File**: `app/src/main/java/com/example/smartfinancialmanagement/StudentDashboardActivity.java`

Add in `initViews()`:
- Find `btnNotifications` and set click listener to call `showNotificationPanelDialog()`

Add new method `showNotificationPanelDialog()` — copy the same implementation from `WorkerDashboardActivity.java` (lines 407-505):
- Inflates `dialog_notifications.xml`
- Queries Firestore `notifications` where `uid` matches and `isUserTargeted == true`
- Shows notifications in a dialog with read/unread styling
- Marks unread notifications as read on display

### 3. Hybrid Dashboard Layout
**File**: `app/src/main/res/layout/activity_student_worker_hybrid_dashboard.xml`

Add a notification bell button to `layoutHeaderActions` (line 92) — insert a new `TextView` with id `btnNotifications` before the existing `btnSecurity` FrameLayout, matching the worker dashboard pattern.

### 4. Hybrid Dashboard Java
**File**: `app/src/main/java/com/example/smartfinancialmanagement/StudentWorkerHybridDashboardActivity.java`

Add in `initViews()` or equivalent setup:
- Find `btnNotifications` and set click listener to call `showNotificationPanelDialog()`

Add new method `showNotificationPanelDialog()` — same implementation as other dashboards.

## Files to Modify
1. `app/src/main/res/layout/activity_student_dashboard.xml` — add id to notification icon
2. `app/src/main/java/com/example/smartfinancialmanagement/StudentDashboardActivity.java` — add click listener + dialog method
3. `app/src/main/res/layout/activity_student_worker_hybrid_dashboard.xml` — add notification button
4. `app/src/main/java/com/example/smartfinancialmanagement/StudentWorkerHybridDashboardActivity.java` — add click listener + dialog method

## Verification
1. Build: `./gradlew assembleDebug`
2. Manual test: Login as Student → tap notification bell → verify dialog shows Firestore notifications
3. Login as Hybrid → tap notification bell → verify same behavior

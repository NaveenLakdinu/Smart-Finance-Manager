<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SmartFinancialManagement" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Widget.App.Button.Green" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/brand_green</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="Widget.App.Button.DarkBlue" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/brand_dark_blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">15dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="Widget.App.EditText" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edittext_rounded_bg</item> <item name="android:padding">15dp</item>
        <item name="android:textSize">14sp</item>
    </style>


</resources>